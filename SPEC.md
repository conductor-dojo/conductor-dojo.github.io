# Keymap Dojo — 仕様と決定ログ

自分のキーマップ JSON を読み込んで、その配列のままタイピングを練習するサイト。

## いまの形

- **1枚の静的HTML**（`index.html`）。サーバー無し、ログイン無し、送信無し。
- キーマップは `FileReader` でブラウザ内だけで読む。`localStorage` に最後の1つを覚える。
- 出題は日本語（ローマ字入力）。盤面が**次に押すキー**と**押さえるレイヤーキー**を光らせる。
- ゲームは1本：全キーぶんの語を1つずつ打ち切るタイムアタック（時間制限なし）。

## ビルド

`index.html` は手書きではなく、Artifact 版（`_source.html`）から組み立てている。

```
node build.js    # 物理配置とキーマップ読み込みを汎用化
node build2.js   # ローダーの配線／アカウント・順位表の撤去
node build3.js   # 注記の汎用化／打てない語の除外
```

`_source.html` を更新したら3本を順に流す。

## 対応するキーマップの形

ZMK 用キーマップエディタが書き出す JSON。

```jsonc
{
  "name": "...",
  "layers": [
    { "id": 0, "name": "base", "color": "#B87333",
      "bindings": { "L00": { "label": "Q", "keyCode": "Q", "type": "basic" }, ... } }
  ],
  "combos": [ ... ]
}
```

- 位置キーは `<側><行><列>`（`L00`, `R23`）。**側・行・列をここから読み取るので、キー数や列数が違う板でも描ける。**
- `type`: `basic` / `mod-tap` / `layer-tap` / `momentary` / `toggle` / `transparent` / `none`
- `holdAction` に `Layer 1` のような文字列があれば、そのキーをレイヤー呼び出しキーとみなす

## 決定ログ

### 2026-09-19 位置キーから盤面を組む
キー数・列数・左右をハードコードせず、`base` レイヤーの位置キー（`L00` 形式）から割り出す。
**理由**: 他人の板を読むのが目的なので、40キー前提の実装では意味がない。最下段（最大の行番号）を
サム/下段として別の行に流し、それより上を格子に並べる。

### 2026-09-19 キーキャップの色は派生させる
オレンジ＝レイヤーを出すキー、グレー＝最下段、他はアイボリー。
**理由**: 元は position を直書きしていた。派生にすると他の板でも意味のある色分けになり、
かつ「オレンジ＝レイヤーキー」という読み方がそのまま通る。

### 2026-09-19 打てない語は出題しない
語の標準ローマ字に、読み込んだ配列で出せない文字が1つでもあれば出題から外す。
**理由**: `-`（長音）が無い配列を読むと、盤面が光らない語が延々出て詰まる。

### 2026-09-19 アカウントと順位表は載せない（この版では）
Artifact 版にはあるが、静的サイトには持ち込まない。
**理由**: 検証したい仮説は「自分のキーマップで練習したい人がいるか」であって
「アカウントが欲しいか」ではない。認証とDBは反応を見てから。

## 未実装（分かっていて置いているもの）

- **英語の出題**。いまは日本語のみ。ZMK 界隈は英語圏が中心なので、公開前に要検討。
- ZMK の `.keymap` ファイル（DTS）の直読み。いまはエディタの JSON のみ。
- レイヤー表・コンボ一覧の表示（読み込んだ配列のぶん）。
- 反応を測る導線（フォーム等）。
